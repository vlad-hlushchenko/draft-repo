# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:

  workflow_dispatch:
    branches:
      - main
  push:
    branches:
      - AQA-add-Some-test
      - test
      - main
      

  # Allows you to run this workflow manually from the Actions tab

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
        

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          ech test, and deploy your project.
      - name: Notify about failure
        if: ${{ failure() }}
        run: |
           cat << EOF > message.json
           {"@type":"MessageCard","@context":"https://schema.org/extensions","summary":"Workflow failed!","themeColor":"ff0000","title":"$GITHUB_REPOSITORY pipeline failed 💢!","sections":[{"facts":[{"name":"Repository:","value":"$GITHUB_REPOSITORY"},{"name":"Branch:","value":"$GITHUB_REF_NAME"},{"name":"Commit:","value":"$GITHUB_SHA"}]}],"potentialAction":[{"@type":"OpenUri","name":"View on GitHub","targets":[{"os":"default","uri":"$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"}]}]}
           EOF
           curl -X POST https://enginebcom.webhook.office.com/webhookb2/abfff974-572e-4ad5-ad94-e827d6c81896@070927bf-7189-4b3c-99b5-60f306d9b138/IncomingWebhook/4580a2741fea4f97bc36d0ddfce39447/8d733b55-6dd3-4b0f-ae02-55b0ca721a4e --header 'Content-Type: application/json' -d @message.json
      - name: Notify about success
        run: |
           cat << EOF > message.json
           {"@type":"MessageCard","@context":"https://schema.org/extensions","summary":"Workflow success!","themeColor":"#5ad027","title":"$GITHUB_REPOSITORY pipeline success ✅!","sections":[{"facts":[{"name":"Repository:","value":"$GITHUB_REPOSITORY"}{"name":"Branch:","value":"$GITHUB_REF_NAME"},{"name":"Commit:","value":"$GITHUB_SHA"}]}],"potentialAction":[{"@type":"OpenUri","name":"View on GitHub","targets":[{"os":"default","uri":"$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"}]}]}
           EOF
           curl -X POST https://enginebcom.webhook.office.com/webhookb2/abfff974-572e-4ad5-ad94-e827d6c81896@070927bf-7189-4b3c-99b5-60f306d9b138/IncomingWebhook/4580a2741fea4f97bc36d0ddfce39447/8d733b55-6dd3-4b0f-ae02-55b0ca721a4e --header 'Content-Type: application/json' -d @message.json
